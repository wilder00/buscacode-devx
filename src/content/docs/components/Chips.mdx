---
title: Buscacode Chips
sidebar:
  label: Chips
---

import React from 'react'
import { Chip } from '@buscacode/chips'
import { ChipBase, ChipBaseDismissible } from './Chips.tsx'
import ShowArea from '@/components/astro/ShowArea.astro'

Chips to be used with buscacode

## Simple use

<ShowArea title="algo">
  <Chip text="a despicable me" />
</ShowArea>

```tsx
<Chip text="a despicable me" />
```

## With onClick

<ShowArea title="algo">
  <ChipBase client:load />
</ShowArea>

```tsx
<Chip
  text="El texto"
  onClick={() => {
    console.log('Los Chips')
  }}
/>
```

## With onClick and dismissible

<ShowArea title="algo">
  <ChipBaseDismissible client:load />
</ShowArea>

```tsx
<Chip
  text="El texto"
  onClick={() => {
    console.log('Los Chips')
  }}
  dismissible
/>
```

## With disabled

<ShowArea title="algo">
  <Chip
    text="El texto"
    onClick={() => {
      console.log('Los Chips')
    }}
    dismissible
    disabled
  />
</ShowArea>

```tsx
<Chip
  text="El texto"
  onClick={() => {
    console.log('Los Chips')
  }}
  dismissible
  disabled
/>
```

## In line

<ShowArea title="algo">

  <div className="max-w-[300px]">
    <p> Let's see if it works <Chip
        text="El texto"
        onClick={() => {
          console.log('Los Chips')
        }}
        dismissible
        disabled
      /> with some texts. Lorem more text to know more behaviors.</p>
  </div>
</ShowArea>

```tsx
<p>
  Let's see if it works{' '}
  <Chip
    text="El texto"
    onClick={() => {
      console.log('Los Chips')
    }}
    dismissible
    disabled
  />{' '}
  with some texts. Lorem more text to know more behaviors.
</p>
```
